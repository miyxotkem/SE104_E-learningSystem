<UserControl x:Class="e_learning_app.Views.QuizHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="#F8FAFC">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#E2E8F0"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40,30,40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel>
                <Button Content="← Quay lại" Background="Transparent" Foreground="#64748B" FontWeight="Medium" 
                        BorderThickness="0" HorizontalAlignment="Left" Cursor="Hand" Click="BtnBack_Click"/>
                
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="TxtExamTitle" Text="Tên bài kiểm tra" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock x:Name="TxtExamDescription" Text="Mô tả bài kiểm tra sẽ hiển thị ở đây." FontSize="14" Foreground="#64748B" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>

                    <Button Grid.Column="1" x:Name="BtnStartQuiz" Content="🚀 Bắt đầu làm bài" Style="{StaticResource ActionButtonStyle}" Click="BtnStartQuiz_Click" VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Grid>

        <!-- Exam Stats Info -->
        <Border Grid.Row="1" Background="White" CornerRadius="16" Padding="25" Margin="0,0,0,30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05" Color="Black"/>
            </Border.Effect>
            <UniformGrid Columns="5">
                <StackPanel>
                    <TextBlock Text="THỜI GIAN" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtTimeLimit" Text="60 phút" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="ĐIỂM ĐẠT" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtPassingScore" Text="50%" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="SỐ CÂU HỎI" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtTotalQuestions" Text="-- câu" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="KỶ LỤC" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtHighestScore" Text="-- / --" FontSize="16" FontWeight="Bold" Foreground="#3B82F6"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="SỐ LẦN CHO PHÉP" FontSize="11" FontWeight="Bold" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtMaxAttempts" Text="3 lần" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                </StackPanel>
            </UniformGrid>
        </Border>

        <!-- Attempt History -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Lịch sử làm bài" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="ItemsHistory" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,12"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="AttemptCard" Background="White" CornerRadius="12" Padding="20" 
                                            BorderBrush="#F1F5F9" BorderThickness="1">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.03" Color="Black"/>
                                        </Border.Effect>
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="AttemptCard" Property="BorderBrush" Value="#3B82F6"/>
                                            <Setter TargetName="AttemptCard" Property="Background" Value="#F8FAFC"/>
                                            <Setter TargetName="AttemptCard" Property="Cursor" Value="Hand"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid MouseLeftButtonUp="AttemptCard_MouseLeftButtonUp">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Attempt Badge -->
                            <Border Grid.Column="0" Width="40" Height="40" Background="#EFF6FF" CornerRadius="8">
                                <TextBlock Text="📝" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding SubmittedAt, StringFormat='Ngày nộp: {0:dd/MM/yyyy HH:mm}'}" FontWeight="SemiBold" Foreground="#334155" FontSize="14"/>
                                <TextBlock Text="{Binding TimeDisplay}" Foreground="#94A3B8" FontSize="12" Margin="0,3,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding ScoreDisplay}" FontSize="18" FontWeight="Bold" Foreground="#3B82F6" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding StatusText}" FontSize="12" FontWeight="SemiBold" Foreground="{Binding StatusBrush}" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
